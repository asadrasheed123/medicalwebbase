import 'package:flutter/material.dart';
class webtokenhome extends StatelessWidget {
  const webtokenhome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('webtoken home'),),
    );
  }
}
